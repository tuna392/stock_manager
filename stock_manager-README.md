# stock_manager

分子生物学研究室向けの在庫管理デスクトップアプリです。細胞の凍結ストック・セルペレット・プライマーを統合管理します。

## 概要

研究室で日常的に扱う生体サンプルの在庫をGUIで管理するアプリケーションです。PyQt5で構築したデスクトップGUIから在庫の登録・使用・削除が可能で、すべての操作はCSV/JSONファイルに永続化されます。macOS / Windows のクロスプラットフォームで動作します。

## 主な機能

- **細胞凍結ストック管理** : 細胞名・継代数・保存場所（液体窒素タンク）・細胞数などを管理
- **セルペレット管理** : ペレットの登録・使用数の記録
- **プライマー管理** : PCRプライマーのID・配列・Application・Animalなどを一覧管理
- **操作ログ** : 追加・使用・削除の全操作を自動記録し閲覧可能
- **メンバー管理** : 保存者・使用者として登録する研究室メンバーの追加・削除
- **インタラクティブな保存場所選択** : 液体窒素タンクのキャニスター・ラック・ボックス位置をGUI上のグリッドから視覚的に選択
- **キーワード検索** : 細胞名での絞り込み検索

## 技術スタック

| 領域 | 技術 |
|------|------|
| GUI | PyQt5 |
| データ永続化 | CSV / JSON |
| 実行環境 | Python 3.x |
| 対応OS | macOS / Windows |

## アーキテクチャ

```
gui_main.py          # GUIレイヤー（ウィンドウ・ダイアログクラス群）
    │
    └── stock_management.py  # ロジックレイヤー（データ読み書き・フィルタリング・ログ記録）
          │
          ├── stock.csv        # 凍結ストックデータ
          ├── pellet.csv       # セルペレットデータ
          ├── primer.csv       # プライマーデータ
          ├── log.csv          # 操作ログ
          └── members.json     # メンバーリスト
```

GUIロジックとデータロジックをレイヤー分離し、保守性を確保しています。

## 工夫した点

- **クロスOS対応のパス解決** : `sys.frozen`とプラットフォームの判定を組み合わせ、macOS `.app` バンドル内でも正しいデータファイルのパスを解決
- **楽観的な確認フロー** : 削除操作では「確認→使用者選択→実行」の2段階ダイアログで誤操作を防止
- **カスタムデリゲート** : `QStyledItemDelegate`を継承し、テーブルのセルを用途に応じてカスタム描画
- **使用場所の視覚的選択** : 液体窒素タンクの構造（キャニスター・ラック・ボックス）をGUIで再現し、保存済みスロットをハイライト表示

## セットアップ

```bash
pip install PyQt5
python gui_main.py
```

初回起動時に `stock.csv`、`pellet.csv`、`primer.csv`、`log.csv`、`members.json` が自動生成されます。

## スクリーンショット

| ホーム画面 | 凍結ストック管理 | 保存場所選択 |
|---|---|---|
| メイン4機能へのナビゲーション | 一覧テーブル・検索・追加・使用・削除 | タンク構造を視覚的に再現したグリッドUI |
